# Frontend runtime config (Vite)
# API mode stays local for now (uses localStorage). When backend is ready, change to "backend".
VITE_API_MODE=local
# Backend base URL/hostname
VITE_API_BASE=http://kiwi-microservice

